# ハンドオフ台帳

- フェーズ: 1 (Fable 著者)
- ベースライン記録: 1 件
- 前回 Fable-off からのタスク数: 1 (5 に達したら次の委譲は Fable-off 訓練)

---

## 2026-07-04 Issue #1/#2 対応 (著者: Fable) — 完了 (ベースライン #1)

- 依頼: 「起票された Issue を対応してください」(Codex へ委譲)
- ドラフト: ユーザードラフトなし。Issue #1/#2 本文 (再現テスト・機構分析・優先度付き提案まで記述済み) をドラフト入力として代用。差分採掘は「Issue の提案」対「指示書で確定したスコープ・停止点」の差分で行う
- スコープ判断 (監督役): Issue #1 提案 1 (ネスト意味論ドキュメント) + 提案 2 (DEBUG 診断) と Issue #2 全提案を実装対象。Issue #1 提案 3 (`.scopeAnimation` API 拡張) は設計スパイク (Go/No-Go 資料) のみとし、公開 API 変更はこの委譲に含めない — 実装可否はスパイク結果を見てユーザーが判断
- アンサンブル批評: 省略 (既知タスク類型・既存コードベース・失敗コスト小)。標準ゲート M0 のみ
- 指示書: `docs/handoffs/2026-07-04-issues-1-2.md`

### 経過メモ (締め時に 4 行 + 4 指標へ集約する)

- M0 ゲート質問 2 件: (1) sandbox が `.git` 書き込み禁止で git 操作不能 → 監督役が全 git 操作を代行する運用に変更。(2) DocC の範囲 — 指示書が `Documentation.docc/` カタログの存在を見落とし **[指示書起因]**
- M1 一発受け入れ: 否。差し戻し 1 件 = 警告の誤発火なしを証明する対照群テストの欠如 (指示書は「非ネストの対照群」としか書いておらず警告 0 件のアサートを明示せず **[一部指示書起因]**)
- M1 差し戻しセッションは外部停止されたため、対照群 2 テストは監督役が直接適用 (役割分離の逸脱として記録)。その対照群が probe view の leaf 依存不足を 1 件検出 — 否定的証明の対照群が実際に機能した実例
- M1 受け入れ検証は監督役が独立再実行: macOS `swift test` 20/20・release build・format lint・iOS Simulator (iPhone 17) 20/20。commit 48c5f7f
- M2 一発受け入れ: 概ね可 (指摘 1 件 = lint レシピの `withAnimation` 検査欠落。Issue 原文には明記されており指示書は「付録 B をベースに」と参照済み → 実装側の読み落とし、指示書起因ではない)。監督役が直接修正。docbuild はリンク警告ゼロを監督役が確認。commit 3dcb7ad

### 締め (4 行 + 4 指標)

- 最初の自分の依頼に足りなかった情報: 「Issue を対応して」のみで、対応範囲 (提案 3 の API 実装を含むか)・git 権限・停止点の指定なし。スコープ確定 (API はスパイクのみ・採否はユーザー判断) は監督役が補った
- ゲート・批評・Fable が補った観点: M0 ゲートが sandbox の git 制約と DocC カタログの存在 (指示書の穴) を着手前に露呈させた。監査が対照群欠如 (M1)・lint レシピの withAnimation 欠落 (M2) を検出。追加した対照群テストが probe view の設計不備をさらに 1 件検出
- 実行側が失敗しかけた点: sandbox 制約 (git 書き込み不可・ビルドキャッシュ・CoreSimulatorService 不通) への回避策探索で消耗。iOS Simulator 検証は最後まで実行不能 → 監督役の独立実行で充足。複合コマンド起動 (`git commit && headroom wrap codex`) で codex が stdin EOF 待ちハング
- 次回からテンプレートに入れるべき項目: (1) Codex sandbox の既知制約と回避策 (git は監督役担当・`CLANG_MODULE_CACHE_PATH` + `--disable-sandbox`・シミュレータ検証は監督役) を指示書 §0/§7 に定型記載、(2) 診断系の受け入れ条件に「誤発火なしの対照群 (警告 0 件アサート)」を明示、(3) codex 起動は単独コマンド + `< /dev/null` に固定

指標: ゲート質問数 2 (全て有効・うち指示書起因 1) / 一発受け入れ 1/3 マイルストーン (M3 のみ。M1 = 対照群欠如で差し戻し、M2 = 軽微 1 件を監督役修正) / 監査指摘数 2 + 対照群による追加検出 1 / 指示書起因指摘 1.5 (DocC 見落とし + 対照群アサート未明示の半分)
トークン: Codex 合計 ~735k (M0 211k / M1 251k / M1 差し戻し中断 / M2 172k / M3 101k)
